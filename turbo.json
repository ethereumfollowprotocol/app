{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"]
    },
    "web#build": {
      "dependsOn": ["^build"],
      "env": ["WALLET_CONNECT_PROJECT_ID", "PUBLIC_WALLET_CONNECT_PROJECT_ID"],
      "outputs": ["dist/**"]
    },
    "lint": {},
    "format": {},
    "typecheck": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  },
  "globalEnv": []
}
